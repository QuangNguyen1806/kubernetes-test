apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: config-secret-reader
  namespace: intern-app
rules:
  - apiGroups: [""]
    resources: ["configmaps", "secrets"]
    verbs: ["get", "list"]
