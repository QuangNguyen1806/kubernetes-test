apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-api3-ns
resources:
  - ../../base
  - servicemonitor.yaml
images:
  - name: demo-api
    newName: demo-api
    newTag: latest
configMapGenerator:
  - name: app-config
    behavior: merge
    literals:
      - APP_NAME=flux-api3
      - REDIS_KEY=flux:api3:items
      - MESSAGE=Hello from Flux api3
patches:
  - path: deployment-patch.yaml
  - path: service-patch.yaml
  - path: hpa-patch.yaml
  - patch: |-
      $patch: delete
      apiVersion: rbac.authorization.k8s.io/v1
      kind: RoleBinding
      metadata:
        name: api3-sa-binding
        namespace: intern-app
