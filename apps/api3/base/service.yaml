apiVersion: v1
kind: Service
metadata:
  name: api3
  labels:
    app: api3
spec:
  type: NodePort
  selector:
    app: api3
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30082
