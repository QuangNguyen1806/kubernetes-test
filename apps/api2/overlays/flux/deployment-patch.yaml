apiVersion: apps/v1
kind: Deployment
metadata:
  name: api2
spec:
  template:
    spec:
      containers:
        - name: api
          imagePullPolicy: Never
          env:
            - name: REDIS_HOST
              value: redis-0.redis.flux-redis-ns.svc.cluster.local
