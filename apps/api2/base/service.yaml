apiVersion: v1
kind: Service
metadata:
  name: api2
  labels:
    app: api2
spec:
  type: NodePort
  selector:
    app: api2
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30081
