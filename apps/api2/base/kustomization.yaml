apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: api2-ns
resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - rbac.yaml
configMapGenerator:
  - name: app-config
    literals:
      - MESSAGE=Hello from api2 ConfigMap
secretGenerator:
  - name: app-secret
    literals:
      - API_KEY=sk-api2-secret-key
  - name: redis-secret
    literals:
      - REDIS_PASSWORD=redis-super-secret-password
    options:
      disableNameSuffixHash: true
